"use client";

import CreditsComponent from "@/components/dashboard/credits/CreditsComponent";

export default function CreditsPage() {
    return (
        <div className="min-h-screen bg-background">
            <CreditsComponent />
        </div>
    );
}